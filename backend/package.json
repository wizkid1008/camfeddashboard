{
  "name": "camfed-dashboard-api",
  "version": "1.0.0",
  "description": "CAMFED Dashboard API — proxies Supabase PostgreSQL to the static frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
